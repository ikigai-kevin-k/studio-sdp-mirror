# Git CI/CD 快速參考卡片

## 🚀 快速使用

### 跳過 CI/CD 流程
```bash
# 使用腳本 (推薦)
./git-commit-helper.sh -s "Your commit message"

# 手動使用
git commit -m "Your commit message [skip-ci]"
```

### 跳過建置流程
```bash
# 使用腳本 (推薦)
./git-commit-helper.sh -n "Your commit message"

# 手動使用
git commit -m "Your commit message [no-build]"
```

### 正常觸發 CI/CD
```bash
# 使用腳本
./git-commit-helper.sh "Your commit message"

# 手動使用
git commit -m "Your commit message"
```

## 📋 可用的標記

| 標記 | 效果 | 使用場景 |
|------|------|----------|
| `[skip-ci]` | 跳過所有 CI/CD 步驟 | 文檔更新、註釋修改 |
| `[no-build]` | 跳過所有 CI/CD 步驟 | 緊急修復、實驗性功能 |
| 無標記 | 正常執行所有步驟 | 正式功能開發、修復 |

## ⚠️ 注意事項

- 只對 `dev/ella/deploy` 分支有效
- 標記必須完全匹配（包括方括號）
- 可以放在 commit message 的任何位置

## 🔧 腳本功能

- 自動檢查分支
- 彩色輸出提示
- 確認對話框
- 參數驗證
- 使用說明

## 📖 完整說明

詳細說明請參考：`GIT_CI_CONTROL_README.md`
